require "spec_helper"
require "bosh/gen/models"

class ExtractSpec < MiniTest::Spec
  it "finds the files to copy from a job with nested dependencies" do
  end
  
  it "finds to files to copy from a package with nested dependencies" do
  end
end
